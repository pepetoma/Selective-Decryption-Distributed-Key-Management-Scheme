# グローバル無視
.DS_Store
.idea/
.vscode/
.env
/build/
/target/

# Codex（念のため、保険。間違って、app/に存在してしまったときのため）
.codex/
auth.json

# Flutter固有
frontend/android/key.properties
frontend/ios/Runner/GeneratedPluginRegistrant.*

# Foundry固有
contracts/out/
contracts/cache/

# ローカル設定
*.local
*.log

# ドキュメント関連
ARCHITECTURE.md
MPC_FLOW.md
CIRCUITS_SPEC.md
ONCHAIN_VERIFY.md
SECURITY_MODEL.md
GLOSSARY.md
PROJECT_OVERVIEW.md
TASKS.md

# Ignore Powers of Tau binaries stored under tools/ptau
# 理由: 大きなバイナリはリポジトリ肥大化とセキュリティの観点からコミットしないため
/tools/ptau/*.ptau
/tools/ptau/*.ptau.*

# Keep the README in the ptau directory tracked
!/tools/ptau/README.md

# Node / npm
node_modules/
.npm/
.npm-cache/

# npm / yarn / pnpm debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Do NOT ignore lockfiles (package-lock.json / yarn.lock / pnpm-lock.yaml) — commit them for reproducible installs
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# Build / output / coverage
dist/
out/
coverage/
.nyc_output/
.eslintcache
*.tsbuildinfo

# Caches used by various bundlers / tools
.cache/
.cache-loader/
.vite/
.parcel-cache/

# Framework/tool specific caches / outputs
.next/
.nuxt/
.serverless/

# Generated/bundled artifacts
*.bundle.js
*.min.js

# Circom ビルド成果物（生成物）
circuits/build/
circuits/**/build/
circuits/**/*.r1cs
circuits/**/*.zkey
circuits/**/*.vkey.json
circuits/**/*.wtns
circuits/**/*.wasm
circuits/**/*.proof.json
circuits/**/*.public.json
circuits/**/manifest.json

# 生成された Verifier（snarkjs 出力）
# 理由: ビルドごとに差分が大きく、再生成可能なため。必要時はCIやリリース工程で生成・配置する。
contracts/src/generated/Verifier.sol

# ダウンロードしたバイナリ（再取得可能）
tools/bin/

# AGENTS.md
AGENTS.md
